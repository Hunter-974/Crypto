import { IfIsLoggedInDirective } from './if-is-logged-in.directive';

describe('IfIsLoggedInDirective', () => {
  it('should create an instance', () => {
    const directive = new IfIsLoggedInDirective();
    expect(directive).toBeTruthy();
  });
});
