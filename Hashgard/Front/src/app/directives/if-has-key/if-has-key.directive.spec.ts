import { IfHasKeyDirective } from './if-has-key.directive';

describe('IfHasKeyDirective', () => {
  it('should create an instance', () => {
    const directive = new IfHasKeyDirective();
    expect(directive).toBeTruthy();
  });
});
