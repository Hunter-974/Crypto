import { IfIsOwnerDirective } from './if-is-owner.directive';

describe('IfIsOwnerDirective', () => {
  it('should create an instance', () => {
    const directive = new IfIsOwnerDirective();
    expect(directive).toBeTruthy();
  });
});
