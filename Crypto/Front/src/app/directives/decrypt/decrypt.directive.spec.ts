import { DecryptDirective } from './decrypt.directive';

describe('DecryptDirective', () => {
  it('should create an instance', () => {
    const directive = new DecryptDirective();
    expect(directive).toBeTruthy();
  });
});
